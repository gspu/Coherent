This src dir only builds the [nt]roff binaries for 4.2. The remainder of the
package (macros, fonts...) reside under /newbits/bin/nroff.

Just cd to the objects dir and export SRCPATH=.., then run 'make'.
