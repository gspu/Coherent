To build the non-ndp and ndp versions of curses, rm the .o's and run
make. You will have to edit CFLAGS in the Makefile to switch between 
building ndp and non-ndp versions of libcurses.a.

The current curses.h and terminfo.h files for /usr/include should come
from THIS directory.
