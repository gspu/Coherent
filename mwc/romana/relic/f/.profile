# /.profile 5/7/90
# COHERENT executes this profile when root logs in.
export PS1='root@romana {$PWD} '
export PS2='root> '
MAIL=/usr/spool/mail/root
export PATH=$PATH:/etc:/usr/lib/uucp:/local/bin:/usr/local/bin
set -o emacs
