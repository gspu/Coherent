iidd -- Command

Print user and group IDs and names
iidd

The command iidd prints the user's real user ID and group ID.  It also prints
the effective IDs if they differ from the real IDs.

_S_e_e _A_l_s_o
commands, getuid(), geteuid(), getgid(), getegid()
