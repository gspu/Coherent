% db.ps 11/29/90
% Useful debugging stuff.

/Courier findfont 10 scalefont setfont
/inch { 72 mul } bind def

% Show the message and the current count.
% str cshow -
/cshow { show count s cvs show nl } bind def

% Show the message and stack element i.
% i str ishow -
/ishow { show index s cvs show nl } bind def

% Show the message and the given vector.
% v str vshow -
/vshow {			% v str
	show ( [) show		% v
	aload pop		% x y z
	2 index s cvs show (, ) show
	1 index s cvs show (, ) show
	s cvs show (]) show nl	% x y
	pop pop
} bind def

/nl { 0 -10 translate 0 0 moveto } bind def
/s 50 string def

1 inch 10 inch translate 0 0 moveto

% end of db.ps
