% lib/clipbox.ps 11/23/90

% Draw box at [x, y] with width dx and height dx and clip in it.
% linewidth dx dy x y clipbox -
/clipbox
{
	newpath
	moveto
	1 index 0 rlineto
	0 exch rlineto
	neg 0 rlineto
	closepath
	gsave setlinewidth stroke grestore
	clip
} bind def

% end of clipbox.ps
