This directory holds the sources for "rdb", a relational data-base product
that was (briefly) sold by MWC.  To rebuild the manual, uncompress all
of the files, then type "Run".  You may have to tinker with the script a
bit, although in main it's pretty sound.

