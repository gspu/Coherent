ftp - Transfer files via the Internet file-transfer protocol

Syntax:	ftp remote.host
		Connect to "remote.host" via FTP

net displays prompt "ftp>"; you can then use the following commands:

	abort		cd		cwd		dele
	dir		get		hash		list
	ls		mkdir		pass		put
	pwd		quit		rmdir		type
	user
