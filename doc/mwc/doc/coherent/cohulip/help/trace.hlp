trace - Trace the handling of datagrams

Syntax:	trace [interface flags [allmode|cmdmode] ]
		"interface" names the interface to trace (e.g., "sl0" or "nx0")
		"flags" sets flags to control tracing
		allmode traces all activity; cmdmode traces activity only
		when 'net' is in command mode
	trace to outputfile
		Write the trace into "outputfile"
