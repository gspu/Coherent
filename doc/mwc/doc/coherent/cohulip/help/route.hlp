route - Manipulate the routing table

Syntax:	route
		Display contents of the routing table
	route drop remote.host
		Drop "remote.host" from the routing table
	route add remote.host[/bits]|default interface [gateway [metric] ]
		Add "remote.host" to routine table.  'default' means make
		a remote host the default recipient of packets (used with SLIP)
		"interface" is the interrface being used (e.g., "sl0" or "ne0")
		"gateway" names the gateway site through which "remote.host"
		is accessed.
