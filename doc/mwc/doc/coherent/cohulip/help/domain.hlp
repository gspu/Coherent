domain - Manipulate information about domains

Syntax:	domain addserver IP.address
		Identify site "IP.address" as a domain-name server
	domain dropserver IP.address
		Drop site "IP.address" from list of domain-name servers
	domain suffix .abc
		Use ".abc" as default syntax to domain name
	domain trace on|off
		Turn tracing on or off
	
