Syntax:  net [startup.file]

Script "startup.file" contains "net" commands that set up the networking
environment.

net recognizes the following commands:

	? 		!		arp		attach
	cd		close		dir		domain
	echo		eol		escape		etherstat
	exit		finger		ftp		ip
	help		hostname	kick		ping
	pwd		record		reset		route
	session		shell		smtp		start
	stop		tcp		telnet		trace
	udp		upload

Each is described in its own help file.
