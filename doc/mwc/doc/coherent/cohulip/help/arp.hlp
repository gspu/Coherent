arp - Manipulate the address resolution protocol (ARP) table

Syntax:	arp
		Display contents of the ARP table
	arp add hostid ether
		Add "hostid" to the ARP table
	arp add hostid ether
		Drop "hostid" to the ARP table
	arp publish hostid ether
		Respond to an ARP query for some other host for "hostid"
