close - Close an interactive session

Syntax:	close [type [session] ]

type - type of session ("ftp" or "telnet")
session - number of the session

If no session is identified, close the most recently opened session.
